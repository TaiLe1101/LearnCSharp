// Khai báo mảng
int[] numbers;

// Khởi tạo mảng với kích thước cố định
numbers = new int[5];

// Khởi tạo mảng và gán giá trị
int[] primes = { 2, 3, 5, 7, 11 };

int[] numbers1 = { 1, 2, 3, 4, 5 };

for (int i = 0; i < numbers1.Length; i++)
{
    Console.WriteLine(numbers1[i]);
}
